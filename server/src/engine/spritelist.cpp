/* Copyright the ORR-C Dev Team */
#include "spritelist.hpp"
//TODO: Make this work

/*void spritelist::init()
{
//	sprite_list = NULL;
	sprite_list = *(new SDL_Surface*[0]);
	sprite_num = 0;
}*/

/*int spritelist::add_sprite(std::string path)
{
	SDL_Surface *temp;
	temp = sprite_list;
	size_t _size = sprite_num + 1;

	memcpy(temp, sprite_list, _size);
	sprite_num++;

	delete [] temp;

	SDL_Surface *temp2 = img_load3(no_teleport_button_path);
	const SDL_Surface *temp3 = NULL;

	sprite_list[sprite_num - 1] = *temp2;
	if(sprite_list[sprite_num - 1] == (temp3))
	{
		std::cout << "Failed loading image\n";
		return 1;
	}
}*/

//spritelist SpriteList;
